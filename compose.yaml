services:
  frontlane:
    image: ${FRONTLANE_SERVER_IMAGE:-ghcr.io/melvishniz/frontlane:latest}
    build: .
    pull_policy: build
    container_name: frontlane
    restart: unless-stopped
    network_mode: host
    cap_drop:
      - ALL
    cap_add:
      - DAC_OVERRIDE
      - NET_ADMIN
      - NET_RAW
    security_opt:
      - no-new-privileges:true
    environment:
      FRONTLANE_LISTEN: "10.77.0.1:8080"
      FRONTLANE_PUBLIC_ENDPOINT: "${FRONTLANE_PUBLIC_ENDPOINT}"
      FRONTLANE_BASE_DOMAIN: "${FRONTLANE_BASE_DOMAIN}"
      FRONTLANE_ADMIN_USER: "${FRONTLANE_ADMIN_USER:-admin}"
      FRONTLANE_ADMIN_PASSWORD: "${FRONTLANE_ADMIN_PASSWORD}"
      FRONTLANE_COOKIE_SECURE: "true"
    volumes:
      - ./data/frontlane:/data/frontlane
      - ./data/wireguard:/etc/wireguard
      - ./data/traefik/dynamic:/data/traefik/dynamic
      - ./data/coredns:/data/coredns

  traefik:
    image: traefik:v3.7.12
    container_name: frontlane-traefik
    restart: unless-stopped
    network_mode: host
    depends_on:
      - frontlane
    command:
      - --entrypoints.web.address=:80
      - --entrypoints.web.http.aliasheadersstrategy=delete
      - --entrypoints.web.http.redirections.entrypoint.to=websecure
      - --entrypoints.web.http.redirections.entrypoint.scheme=https
      - --entrypoints.websecure.address=:443
      - --entrypoints.websecure.http.aliasheadersstrategy=delete
      - --providers.providersthrottleduration=500ms
      - --providers.file.directory=/etc/traefik/dynamic
      - --providers.file.watch=true
      - --certificatesresolvers.letsencrypt.acme.email=${FRONTLANE_ACME_EMAIL:?FRONTLANE_ACME_EMAIL is required}
      - --certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json
      - --certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web
      - --api.dashboard=true
      - --api.basepath=/traefik
      - --accesslog=true
      - --global.checknewversion=false
      - --global.sendanonymoususage=false
    volumes:
      - ./data/traefik/dynamic:/etc/traefik/dynamic:ro
      - ./data/traefik/letsencrypt:/letsencrypt

  coredns:
    image: coredns/coredns:1.12.0
    container_name: frontlane-coredns
    restart: unless-stopped
    network_mode: host
    depends_on:
      - frontlane
    command: -conf /etc/coredns/Corefile
    volumes:
      - ./deploy/coredns/Corefile:/etc/coredns/Corefile:ro
      - ./data/coredns:/data/coredns:ro

  app-example:
    image: nginx:1.29-alpine
    container_name: frontlane-app-example
    restart: unless-stopped
    profiles: ["example"]
    ports:
      - "127.0.0.1:8081:80"
